### Windows
# Windows thumbnail cache files
Thumbs.db

# Folder config file
[Dd]esktop.ini

# Recycle Bin used on file shares
$RECYCLE.BIN/

# Windows shortcuts
*.lnk
PiScript/terminalcommands.txt

### .NET build output
# Regenerated by every `dotnet build`/`dotnet run` - shouldn't be tracked.
# gps_log.jsonl lives under bin/ at runtime, so it's covered by this too.
bin/
obj/
